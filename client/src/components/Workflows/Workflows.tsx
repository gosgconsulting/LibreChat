import React from 'react';

export default function Workflows() {
  return (
    <div className="w-full h-full">
      {/* Blank page reserved for future workflow builder (e.g., SEO Workflow, n8n integration) */}
    </div>
  );
}
